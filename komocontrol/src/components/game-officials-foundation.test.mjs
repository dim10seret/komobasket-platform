import { readFileSync } from "node:fs";
import { expect, test } from "vitest";
const source = (path) => readFileSync(new URL(path, import.meta.url), "utf8");
test("old packages and configurations default officials to empty", () => { const packages = source("../../electron/games/game-package-download.cts"); const setup = source("../../electron/games/match-setup.cts"); const configuration = source("../../electron/runs/pre-game-configuration.cts"); expect(packages).toMatch(/officials\?: GamePackageV1Officials/); expect(packages).toMatch(/if \(value === undefined\) return/); expect(setup).toMatch(/if \(!value\) return \{ referees: \{ a: null, b: null, c: null \}/); expect(configuration).toMatch(/if \(value === undefined\) return \{ referees: \{ a: null, b: null, c: null \}/); });
test("KomoControl stores Run-only officials in the existing draft", () => { const component = source("./PreGameConfiguration.tsx"); const configuration = source("../../electron/runs/pre-game-configuration.cts"); expect(component).toContain("Διαιτητές &amp; Γραμματεία"); expect(component).toMatch(/officials: draft\.officials/); expect(configuration).toMatch(/input\.officials === undefined \? current\.officials/); });
test("the same officials editor durably saves pre-game and ACTIVE Run values", () => { const component = source("./PreGameConfiguration.tsx"); const app = source("../App.tsx"); expect(component).toContain("Διόρθωση ενεργού αγώνα"); expect(component.match(/<OfficialsDialog/g)).toHaveLength(1); expect(component).toMatch(/const saved = await onSave\(\{ gameId: configuration\.gameId/); expect(app).toMatch(/savePreGameConfiguration\(input: KomoControlPreGameConfigurationSaveDraftInput\): Promise<boolean>/); });
test("officials modal has an opaque surface above LiveControl", () => { const styles = source("../styles/global.css"); expect(styles).toMatch(/\.pregame-officials-backdrop \{[^}]*z-index: 120;[^}]*background: rgb\(3 7 18 \/ \.78\);[^}]*backdrop-filter: blur\(5px\);/); expect(styles).toMatch(/\.pregame-officials-dialog \{[^}]*background: #fff;/); expect(styles).not.toMatch(/\.pregame-officials-dialog \{[^}]*background: var\(--surface\);/); });
